using UnityEngine;

public class HannController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Hello I am Happy Boi ");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
