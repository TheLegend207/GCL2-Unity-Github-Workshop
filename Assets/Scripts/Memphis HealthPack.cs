using UnityEngine;

public class MemphisHealthPack : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
      void Start()
    {
        print("Health Restored!");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
