using UnityEngine;

public class JoshuaController : MonoBehaviour
{
    public int lives;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print ("Mystery Box activated!");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
