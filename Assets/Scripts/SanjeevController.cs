using UnityEngine;

public class SanjeevController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Enemy Activated");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
